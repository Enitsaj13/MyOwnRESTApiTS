/* eslint-disable */
const unUsedVariables = 16;

/* eslint-enable */
export const Hello = (number: any) => {
  const Addition = number + 5;
  console.log(Addition);
};

export function add(a: number, b: number) {
  return a + b;
}
